import "ifj25" for Ifj

class Program {
    static main() {
        var a
        var b
        var c
        var d
        var e
        var f

       a = 3 * (b + 5) - 10 / (c - 2) * (d + (e - f))

       b = ((a * (b - c)) / (d + e * (f - 1))) - (3 - 2 * (a + 4))

       c = (((a + b) * (c - d)) / ((e * 2) - (f / 3))) + (a - (b * (c / (d + e))))


       // d = -(a * b) + (c / -(d - e)) * (f + 1) takyto este nevie celkovo - parser nevie moc spracovat 

    }
}
