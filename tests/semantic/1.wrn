import "ifj25" for Ifj

class Program {
    static add(x, y) {
        return x + y
    }

    static mul(a, b) {
        return a * b
    }

    static compute(a, b, c) {
        var result
        result = (a + b) * (c - 1)
        return result
    }

    static main() {
        var a
        var b
        var c
        var d
        var e
        var f

        a = (1 + 2) * (3 + 4)
        b = add(a, 10)
        c = mul(b, 2)
        d = compute(c, 3, b)
        e = (d + 5) / (2 * (b - 1))

        var i
        i = 0 

    }
}
